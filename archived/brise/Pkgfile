name=brise
version=git
desc="Rime schema repository"
deps="librime"

ext() {
  git clone --depth=1 https://github.com/rime/brise
  cd brise
  git submodule init
  git submodule update
}

build() {
  cd brise
  make
  make PREFIX=/ DESTDIR="$pkgdir" install
}
