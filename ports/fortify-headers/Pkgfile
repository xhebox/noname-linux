name=fortify-headers
version=0.7
deps="filesystem"
desc="A standalone fortify source implementation"
source="http://dl.2f30.org/releases/$name-$version.tar.gz"

build() {
	cd $name-$version

	make PREFIX= DESTDIR="$pkgdir" install  || die
}
