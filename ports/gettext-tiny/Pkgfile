name=gettext-tiny
version=git
desc="lightweight replacements of the gnu gettext"

ext(){
	git clone https://github.com/sabotage-linux/gettext-tiny
	cd gettext-tiny
}

build(){
	cd gettext-tiny
	make LIBINTL=MUSL -j1 prefix=/ || die
	make LIBINTL=MUSL -j1 DESTDIR=$pkgdir prefix=/ install  || die
}
