name=iso-codes
version=3.74
deps="python3 gettext"
desc="ISO Country/Language/Currency codes"
source="http://pkg-isocodes.alioth.debian.org/downloads/$name-$version.tar.xz"

build() {
	cd $name-$version
	./configure --prefix= --disable-static
	make || die
	make DESTDIR="$pkgdir" install || die
}
