name=libconfig
version=1.6
desc="C/C++ Configuration File Library"
deps=gcc
source="https://github.com/hyperrealm/libconfig/archive/v$version.tar.gz"

build() {
	cd $name-$version
	rm lib/scanner.c
	./configure --prefix= MAKEINFO=true
	make || die
	make DESTDIR="$pkgdir" install || die
}
