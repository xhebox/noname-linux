name=libice
version=1.0.9
desc="xorg lib libICE"
deps="xorg-proto xtrans"
source="http://xorg.freedesktop.org/releases/individual/lib/libICE-$version.tar.bz2"

build() {
	cd libICE-$version

	./configure --prefix= --disable-static

	make || die
	make DESTDIR="$pkgdir" install || die
}

