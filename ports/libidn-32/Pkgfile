. ../libidn/Pkgfile
name=libidn-32

build() {
	cd libidn-$version
	./configure --prefix= --libdir=/lib32 CC='gcc -m32'
	make || die
	make DESTDIR=${pkgdir} install || die
	rm -rf ${pkgdir}/bin
	rm -rf ${pkgdir}/include
	rm -rf ${pkgdir}/share
}
