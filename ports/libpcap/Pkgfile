name=libpcap
version=1.8.1
deps="flex bison libnl"
desc="Packet capture library"
source="http://www.tcpdump.org/release/$name-$version.tar.gz"

build() {
	cd $name-$version
	bash ./configure --prefix= --disable-dbus
	make || die
	make DESTDIR="$pkgdir" install install-shared || die
}
