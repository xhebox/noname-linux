name=libtiff
version=4.0.8
desc="Library for manipulation of TIFF images"
deps="libjpeg-turbo zlib xz glu freeglut mesa"
source="http://download.osgeo.org/libtiff/tiff-${version}.tar.gz"

build() {
	cd tiff-$version
	./configure --prefix=
	make || die
	make DESTDIR="${pkgdir}" install || die
}
